# Deploy v2 Parity Report

Generated for C3 on 2026-06-29.

## Fixture

- Fleet input: `test/fixtures/deployment-v2/import/fleet.yaml`
- Golden v1 tree: `fixtures/deployment-v2/golden/cluster/flux`
- V2 path: `import-fleet-v1` writes persisted v2 inputs, then `compile --env production` renders `cluster/flux`
- Parity ignore set: `GitRepository.spec.url`, `GitRepository.spec.ref.branch`, root `Kustomization.spec.path`

The golden tree is copied from the repo's v1 import fixture tree, not generated by the deploy-v2 renderer.

## Verdict

Full zero-diff parity is not achieved yet.

Current known-good persisted-input subset:

- `autoscaling/v2/HorizontalPodAutoscaler/apps/web-api`
- `v1/ConfigMap/apps/web-api-config`
- `v1/PersistentVolumeClaim/apps/web-api-data`

Summary:

```json
{
  "currentObjects": 13,
  "renderedObjects": 16,
  "missing": 4,
  "extra": 7,
  "changed": 6,
  "duplicates": 0
}
```

## Remaining Diffs

Missing from v2 render:

- `_path/apps/stateless/kustomization.yaml#0`
- `kustomize.toolkit.fluxcd.io/v1/Kustomization/flux-system/apps-core`
- `kustomize.toolkit.fluxcd.io/v1/Kustomization/flux-system/apps-stateless`
- `networking.k8s.io/v1/NetworkPolicy/apps/web-api-ingress`

Extra in v2 render:

- `_path/apps/vso-secrets/kustomization.yaml#0`
- `secrets.hashicorp.com/v1beta1/VaultAuth/vso-secrets/vault-auth`
- `secrets.hashicorp.com/v1beta1/VaultConnection/vso-secrets/vault`
- `v1/ConfigMap/observability/gatus-endpoints`
- `v1/Namespace/_cluster/apps`
- `v1/ServiceAccount/apps/vault-secrets-operator`
- `v1/ServiceAccount/apps/web-api`

Changed objects:

- `_path/apps/stateless/web-api/kustomization.yaml#0`: v2 expands resources to generated namespace, service account, deployment, configmap, pvc, hpa, and servicemonitor files instead of the v1 `workload.yaml`.
- `apps/v1/Deployment/apps/web-api`: v2 adds rollout strategy, config env mapping, secret key casing, port protocol, restart policy, and explicit PVC volume.
- `monitoring.coreos.com/v1/ServiceMonitor/apps/web-api`: v2 adds metrics-stack label, scheme, job label, and selector.
- `secrets.hashicorp.com/v1beta1/VaultStaticSecret/apps/web-api-db`: v2 adds VSO mount/path/refresh/auth and rollout restart fields.
- `traefik.io/v1alpha1/IngressRoute/edge/web-api`: v2 adds public ingress annotations, entryPoint, forward-auth middleware, and TLS.
- `v1/Service/apps/web-api`: v2 adds port protocol and selector.

apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: "${TRAEFIK_LAN_HELM_REPOSITORY_NAME}"
  namespace: "${TRAEFIK_LAN_NAMESPACE}"
spec:
  interval: "${TRAEFIK_LAN_HELM_REPOSITORY_INTERVAL}"
  url: "${TRAEFIK_LAN_HELM_REPOSITORY_URL}"

apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: "${VSO_HELM_REPOSITORY_NAME}"
  namespace: "${VSO_NAMESPACE}"
spec:
  interval: "${VSO_HELM_REPOSITORY_INTERVAL}"
  url: "${VSO_HELM_REPOSITORY_URL}"

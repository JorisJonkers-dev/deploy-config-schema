apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: "${TRAEFIK_PUBLIC_HELM_REPOSITORY_NAME}"
  namespace: "${TRAEFIK_PUBLIC_NAMESPACE}"
spec:
  interval: "${TRAEFIK_PUBLIC_HELM_REPOSITORY_INTERVAL}"
  url: "${TRAEFIK_PUBLIC_HELM_REPOSITORY_URL}"

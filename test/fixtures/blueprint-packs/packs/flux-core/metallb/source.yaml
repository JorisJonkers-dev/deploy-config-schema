apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: "${METALLB_HELM_REPOSITORY_NAME}"
  namespace: "${METALLB_NAMESPACE}"
spec:
  interval: "${METALLB_HELM_REPOSITORY_INTERVAL}"
  url: "${METALLB_HELM_REPOSITORY_URL}"

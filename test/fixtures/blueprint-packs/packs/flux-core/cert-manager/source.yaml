apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: "${CERT_MANAGER_HELM_REPOSITORY_NAME}"
  namespace: "${CERT_MANAGER_NAMESPACE}"
spec:
  interval: "${CERT_MANAGER_HELM_REPOSITORY_INTERVAL}"
  url: "${CERT_MANAGER_HELM_REPOSITORY_URL}"

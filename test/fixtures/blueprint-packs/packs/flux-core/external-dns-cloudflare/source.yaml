apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: "${EXTERNAL_DNS_HELM_REPOSITORY_NAME}"
  namespace: "${EXTERNAL_DNS_NAMESPACE}"
spec:
  interval: "${EXTERNAL_DNS_HELM_REPOSITORY_INTERVAL}"
  url: "${EXTERNAL_DNS_HELM_REPOSITORY_URL}"

apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  name: "${EDGE_DEFAULT_TLS_STORE_NAME}"
  namespace: "${EDGE_NAMESPACE}"
spec:
  defaultCertificate:
    secretName: "${EDGE_DEFAULT_TLS_SECRET_NAME}"

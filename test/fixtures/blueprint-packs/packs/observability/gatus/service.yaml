apiVersion: v1
kind: Service
metadata:
  name: "${GATUS_SERVICE_NAME}"
  namespace: "${GATUS_NAMESPACE}"
spec:
  selector:
    app.kubernetes.io/name: "${GATUS_APP_LABEL}"
  ports:
    - name: http
      port: ${GATUS_SERVICE_PORT}
      targetPort: http
